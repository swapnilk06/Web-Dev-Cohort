// Array - storing a list of data items under a single variable name. Here we look at why this is useful, then explore how to create an array, retrieve, add, and remove items stored in an array.

// - What an array is — a structure that holds a list of variables.

// - The syntax of arrays — [a, b, c] and the accessor syntax, myArray[x].

// - Modifying array values with myArray[x] = y.

// - Array manipulation using common properties and methods such as length, push(), pop(), join(), and split().

// - Advanced array methods such as forEach(), map(), and filter().

// Array - list-like objects
// - single objects that contain multiple values stored in a list.
// - If we didn't have arrays, we'd have to store every item in a separate variable, then call the code that does the printing and adding separately for each item. This would be much longer to write out, less efficient, and more error-prone.

// store a shopping list in an array
const shopping = ["bread", "milk", "cheese", "hummus", "noodles"];
//console.log(shopping);

// in an array we can store various data types — strings, numbers, objects, and even other arrays. We can also mix data types in a single array — we do not have to limit ourselves to storing only numbers in one array, and in another only strings.
const sequence = [1, 1, 2, 3, 5, 8, 13];
const random = ["tree", 795, [0, 1, 2]];




