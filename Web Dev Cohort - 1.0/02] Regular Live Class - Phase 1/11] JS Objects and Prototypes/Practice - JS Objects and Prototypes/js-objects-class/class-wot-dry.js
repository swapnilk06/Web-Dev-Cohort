// Create class in modern JS

// Person class object
class Person {
	// need properties
	constructor(fname, lname) {
    // person class return fname & lname are user 
		this.fname = fname;
		this.lname = lname;
	}
}

// create 2 objects with class
const p1 = new Person("Piyush", `Garg`);
const p1 = new Person(`Akash`, `Kadlag`);

p1. 